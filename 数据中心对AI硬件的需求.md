# 数据中心对 AI 硬件的需求

> 投资研究知识储备。基于李博杰《深入理解 AI Infra》第 1、3、4、6、7 章整理，数字均出自原书，2026-09-24。
> 读法：赶时间读 0、1、7 三节就够，做标的研究按 2 到 6 节对号入座。

## 0️⃣ 从一笔账开始

先算一笔账。70B 模型，权重压到每参数 1 字节，总共 70GB。生成每个 token，加速器要把这 70GB 从内存总线里完整读一遍。读一次 20.9ms，计算只花 0.14ms，差了 148 倍。

这笔账说明一件事：decode 阶段，加速器的时间几乎全花在搬数据上。FLOPs 是卖卡时写进 spec 的数字，也是真正消耗时间的数字，两者在 decode 里是脱钩的。

另一笔账在 scale-up 侧。8 卡 TP8，一步 5.15ms；跨两台机器做 TP16，一步 6.67ms，反而更慢。慢的元凶是跨机链路时延，IB 3.76μs，NVLink 0.822μs，加卡的收益被这零点几微秒吃掉了。

这两笔账是全文的两条主轴。一条决定单卡内部的内存和算力需求，一条决定卡外的互连和机柜需求。后面所有内容都挂在这两条轴上。

## 1️⃣ 需求全景：六层

数据中心的 AI 硬件需求可以拆成六层，从底到顶：

| 层 | 解决什么 | 代表硬件 | 需求弹性由什么驱动 |
|---|---|---|---|
| 算力 | prefill 和训练的 FLOPs | Tensor Core，FP8 / FP4 低精度 | 模型规模、RL 循环 |
| 内存 | decode 的带宽和容量 | HBM 堆栈 | batch 大小、KV 缓存、MoE 权重容量 |
| scale-up 互连 | 超节点内跨卡通信 | NVLink、NVSwitch、华为 UB | 超节点域规模 8 → 72 → 384 |
| scale-out 网络 | 跨机柜、跨集群通信 | IB / RoCE、Clos、光模块、SHARP | 集群规模、超售比 |
| 供电散热 | TDP 和供风的物理上限 | 液冷、120kW 机柜 | 单卡 TDP 700W → 1000W |
| 系统集成 | 把以上全部装进一个机柜 | DGX、GB200 NVL72、CloudMatrix384 | 超节点整机标准 |

做投研时有个习惯很好用：把每一家供应链公司放进这张表，问三件事，属于哪一层，卖的是什么，需求弹性由哪个变量驱动。三个都答得上来，这家公司的逻辑基本就清楚了；第三个答不上来，说明需求端没真正看懂，只是跟着股价在猜。

## 2️⃣ 两种负载，两种卡

**prefill 吃算力，decode 吃内存带宽。** 前者把输入并行处理，arithmetic intensity 高，FLOPs 是主要消耗；后者每个 token 都要把整套权重读一遍，瓶颈在内存带宽。

转折点是可计算的。B* 是算力时间与内存时间相等的 batch，某配置下约 148。低于这个 batch，一张卡绝大部分时间在搬权重，买再多 FLOPs 也闲置。

能量账：batch 1 时每生成一个 token 0.534 焦耳，其中 90% 花在权重读取；batch 提到 32，0.068 焦耳每 token，8 倍差距。vLLM 实测 MBU 从 batch 1 的 35.3% 升到 batch 64 的 65%。

投资含义在这里。H100 和 H200 是两张不同的卡。H200 相对 H100 只加了一件事，更多 HBM：算力没变，都是约 989 TFLOP/s，内存 80GB 到 141GB，带宽 3.35 到 4.8TB/s。decode 占比高的云厂商买的是内存，同一份采购单，在不同负载结构的手里，是两种硬件需求。这是看 AI 硬件财报时最容易踩的坑：把 H200 当 H100 的迭代版本去线性外推。

## 3️⃣ HBM：需求里的增长极

为什么内存是增长极，看每读一字节数据的能量：

| 存储层级 | 每字节能量 |
|---|---|
| 片上 SRAM | 1.25 pJ |
| 片外 SRAM | 12.5 pJ |
| HBM2 | 31.76 pJ |
| LPDDR | 160 pJ |

一条 FMA 是 0.75 pJ，HBM 读一字节要花掉四十多条 FMA 的能量。decode 成本降不下来的情况下，杠杆只有两个：加大 batch 摊薄权重读取，或者降低精度减少字节数。

另一个反直觉的数字：NVLink C2C 传一字节 10.4 pJ，比 HBM 读一字节还便宜。跨卡搬数据比本地读内存省能量，这是超节点在物理层的合理性，也是下一节的地基。

规格演进：

| 卡 | HBM 配置 | 容量 | 带宽 | BF16 | TDP |
|---|---|---|---|---|---|
| H100 | 5 × 16GB | 80GB | 3.35TB/s | 989 TFLOP/s | 700W |
| H200 | 6 × 24GB | 141GB | 4.8TB/s | 989 TFLOP/s | — |
| B200 | 8 × 24GB | 180GB | 8TB/s | — | 1000W |

两个细节值得盯。B200 用 10TB/s 的 NV-HBI 把两个 die 连成一个内存池，容量上限不再由单 die 决定，这条路线下 HBM 需求的增速会高于卡数增速。另一边模型权重在涨：Qwen3-235B 的 BF16 权重 470.2GB，单卡装不下，8 张 H100 共 640GB 刚好放下，每卡 61.5GB。沿着这个斜率，每代卡的 HBM 容量是单调递增的变量，470GB 之后是多少，直接对应 HBM 厂商的营收曲线。

## 4️⃣ 超节点：scale-up 重新定义需求单位

需求的计量单位正在从单卡变成超节点。

最硬的证据是 TP 计算。Qwen3-235B batch 1，TP1 一步 29.35ms，TP8 缩到 5.15ms，5.7 倍；跨两机做 TP16，一步 6.67ms，比 TP8 还慢。Ring AllReduce 的延迟项是 2(n−1)α，α 是链路时延，NVLink 0.822μs 对 IB 3.76μs。跨机那一刻，加卡的收益被链路时延吃掉，这就是超节点存在的原因。

各家在抢的是全 NVLink 域的规模：

| 方案 | scale-up 域 | 结构 |
|---|---|---|
| DGX H100 | 8 卡 | 2:1 收敛 |
| GB200 NVL72 | 72 卡每机柜 | 36 Grace + 72 Blackwell |
| NVLink Switch System | 256 张 H100 以内 | 64 口 NVSwitch |
| CloudMatrix384 | 384 × 910C + 192 Kunpeng | 华为统一总线 |

域内有两种拓扑。TPU v4 用 4×4×4 三维环面加 OCS 光交换，ICI 6 × 50GB/s；NVIDIA 和华为用交换 fabric。实测 uniform All-to-All，环面 336μs，fabric 112μs，3 倍差距，512 卡时拉到 6 倍。做全互联密集负载的，拓扑选型是实打实的性能项。

华为 UB 是另一条切法：把网络控制器直接做到芯片总线上，跳过 PCIe 网卡这一层。64B 远端读 0.42μs，PCIe 网卡 2.2μs；1024 个端点的连接状态表 108KiB 对 512MiB，近 5000 倍；建立一条连接 16ms 对 17 秒，千倍。超节点的两个成本，时延和控制平面，这条路线都正面打了。

我认为这本书里对超节点最有杀伤力的一组数字是 V4.1 Flash 的实测。64 卡超节点上，吞吐是 8 卡配置的 7.4 倍，单用户生成速度不变，362 token/s。同样的 64 张卡散在 8 台机器上，7/8 的调度字节要过网卡，吞吐从 19,420 token/s 掉到 7,828。硬件一份，拓扑两种，收入能力差 6 成。

还有两层隐藏需求。一是内存池化：用 UB 借远端 HBM，KV 缓存溢出时借用别的卡的内存，HBM 的采购量从此和超节点规模正相关，域越大买越多。二是供电：72 卡 pod 的功耗把风冷顶穿，直接拉出液冷市场，第 5 节展开。

## 5️⃣ 网络与电力：约束规模的两层

scale-out 和 scale-up 之间是一个 9 倍的带宽台阶：NVLink 单向 450GB/s，CX-7 单网卡 50GB/s。192MiB 的梯度、3ms 的通信预算，跨机通信只能做减法，能减的杠杆都在这一层：

| 杠杆 | 数据 |
|---|---|
| 层级 reduce | 跨服流量 720 → 384MiB，7.6ms → 1.3ms |
| 网内计算 SHARP | 32 服务器 reduce 1.027ms → 0.504ms |
| Clos 超售比 | 1:1 时 1024 卡 bisection 51.2TB/s，3:1 时 17.6TB/s |
| ECMP 哈希冲突 | 最坏 32 流 32 上联，只剩无冲突速度的 28.3% |
| 包级喷洒 | 8MiB 拆 2048 个 4KiB 包走 8 条路径，29μs 对单路径 168.8μs |

超售比是一个能直接谈钱的变量。3:1 省交换机成本，但 1024 卡集群的 bisection 带宽砍掉三分之二，作业一碰 All-to-All 或 incast，省下的钱会以等待时间还回去。超以太网 UEC 的熵喷洒加选择性重传，是哈希冲突的解法，下一代 RoCE 标准往这个方向走。

1024 卡训练放在这张网上算：TP8 × DP128，MFU 41%。8 卡 pod 一步 0.939 秒，256 卡 pod 一步 0.663 秒，每 token 吞吐 1.117M 到 1.581M tok/s；pod 从 8 扩到 64，梯度同步从 0.318 秒降到 69ms。单 pod 的出口带宽是固定的，扩 pod 就是在堆出口总量，网络这层的营收跟着 pod 数走。

电力侧是硬数字：

| 项目 | 数据 |
|---|---|
| DGX H100 整机 | 10.2kW |
| 120kW 机柜 | 装 88 张 H100 |
| 风冷上限 | 约 40kW 每机柜 |
| 72 卡 pod 供风 | 5000 cfm，普通机柜 1900 cfm |

700W 的卡 8 张一箱，5.6kW，风冷还兜得住。1000W 的 B200 一机柜 72 张，远超 40kW 的风冷线，液冷是超节点机柜的入场券。这层是供应链里最不显眼的，也是物理约束最死的：算力能堆，内存能堆，电网不能。

故障也是需求。Meta 1024 卡作业平均宕机 7.9 小时，128 卡 pod 故障恢复 188 秒，256 卡 pod 316 秒。集群越大，任意一张卡坏掉的概率越高，冗余容量得按概率配。这部分冗余是固定成本，规模越大占比越高，买冗余的人在为别人的故障率付费。

## 6️⃣ 需求驱动：五个把规模往上推的变量

1. 模型规模。训练状态 18 字节每参数，是推理权重的 9 倍，Qwen3-8B 就要 147.4GB，单卡装不下。训练开支还在涨：Llama1-65B 102 万 A100 时，DeepSeek-V3 266 万 H800 时，Llama3.1-405B 3,080 万 H100 时，16,384 张卡训了约 78 天。

2. Token 数。Scaling Law 的 D/N 从 Llama1 的约 149 tokens/param 爬到 Qwen3 的约 4,500，30 倍。实际花费超出 compute-optimal 一到两个数量级，原因是厂商把推理成本内化进了训练预算，训练优化的是训练加推理的全生命周期。

3. MoE。235B 参数只激活 22B，权重容量的需求按 235B 算，算力的需求按 22B 算。这个解耦是 HBM 比 FLOPs 稀缺的原因，也是超节点扩容时单用户速度不降的原因。

4. Agent 和推理负载。三笔账：单请求的 thinking token 有 2,553 个，输出总共 2,733 个，decode 大头是模型内部推理；工具调用等待期间 KV 缓存被钉在内存里，占着内存不产生吞吐；prefix cache 命中率 4480/5297，计算量从 76.2 压到 12.0 TFLOPs。多模态请求里一张 640² 图片变成 400 个视觉 token，视觉 KV 每 token 144KiB，KV 池的压力跟着图片数量走。Agent 负载把需求结构往内存侧推，batch 需求涨，计算强度降。

5. RL。样本保留率从 1/2 降到 1/4，单步 FLOPs 涨 56%，2,181.6 到 3,410.7 TFLOPs。Qwen3-8B 的 RL 训练 17,920 GPU 时，蒸馏路线 1,800 小时，10 倍差。RL 路线如果成为主流，训练需求跟着奖励模型规模走，不再跟着基座模型走。

还有一条反馈回路：DeepSeek V3 发布时明确提出了硬件需求，通信卸载、低精度累加。模型和硬件进入共同设计阶段，需求清单跟着每一代模型变。

最后记一个投研该存档的数字：自研 AI 芯片的盈亏平衡。按当前 1 年的模型生命周期，新架构要跑约 254 台机器才能回本。未来一两年，AI 硬件的需求端还是由供应链承接，自研芯片是稀释供应链份额的变量，值得跟踪，还没到威胁的程度。

## 7️⃣ 投研视角：价值捕获与跟踪指标

价值捕获按第 1 节的六层归位：

| 层 | 价值捕获 | 逻辑 | 风险 |
|---|---|---|---|
| HBM | 内存厂 | 每代容量带宽都在涨，模型规模单调递增 | 新内存路线、低端 LPDDR 替代 |
| scale-up | NVLink Switch、CPO、UB | 8 → 72 → 384 卡，单机柜交换含量上升 | 环面路线、封装内互连 |
| scale-out | 交换机、网卡、光模块 | 集群规模和超售比选择 | UEC 对 IB 的标准之争、网内计算 |
| 供电散热 | 液冷、配电 | 1000W 卡加 72 卡机柜，风冷触到物理上限 | 电网建设进度滞后 |
| 集成 | 超节点整机 | NVL72、CloudMatrix384，按机柜卖标准件 | 自研整机替代 |

**瓶颈迁移**是我给这组标的排的优先级依据。每一代扩张，最紧的一层往后退：算力、带宽、互连规模、电力，这本书的案例顺序就是这个序列。对投资的含义：当前共识最拥挤的那一层，紧张通常已经定价了，要盯的是下一层里供给曲线还平、需求曲线还陡的位置。

跟踪指标，四个数字都能从公开披露和论文里拿到：

| 指标 | 度量什么 | 参考线 |
|---|---|---|
| MBU / MFU | 卡的真实利用率 | 1024 卡训练 41% MFU；decode MBU 35% 到 65% 随 batch 走 |
| 每卡跨域字节 | 互连负载 | 7/8 调度字节跨网卡是收入掉 6 成的一线 |
| 超售比 | 网络冗余 | 1:1 贵、3:1 便宜，看大厂实际采购选择 |
| 单卡 TDP | 电力硬上限 | 700W → 1000W，下一代看液冷渗透率 |

书里四个误区，转过来正好是投研尽调清单。相同总字节不等于相同时长，延迟结构不同；平均利用率低不等于没问题，incast 瞬间才是压力点；有序返回不等于数据正确，网内计算有一整类正确性问题；通信加速不等于任务加速，7.4 倍通信不折算成 7.4 倍收入。公司 PR 里报通信带宽的时候，先翻译成这四个数字再下结论。

## 8️⃣ 关键数字速查

| 项 | 值 |
|---|---|
| 70B / 1 字节 decode | 权重读 20.9ms，计算 0.14ms，148 倍 |
| decode 能耗 | batch 1 0.534 J/token，batch 32 0.068 |
| H100 | 80GB / 3.35TB/s / 989 TFLOP/s / 700W |
| H200 | 141GB / 4.8TB/s / 989 TFLOP/s |
| B200 | 180GB / 8TB/s / NV-HBI 10TB/s / 1000W |
| NVLink 单向 | 450GB/s，A100 为 300GB/s |
| CX-7 单向 | 50GB/s |
| NVLink / IB 时延 | 0.822μs / 3.76μs |
| UB 远端读 | 0.42μs 对 PCIe 2.2μs |
| DGX H100 整机 | 10.2kW |
| 120kW 机柜 | 88 张 H100 |
| 风冷上限 | 约 40kW 每机柜 |
| NVL72 | 36 Grace + 72 Blackwell 每机柜 |
| CloudMatrix384 | 384 × 910C + 192 Kunpeng |
| 训练状态 | 18 字节每参数 |
| 1024 卡 bisection | 51.2TB/s 1:1，17.6TB/s 3:1 |
| 1024 卡训练吞吐 | 8 卡 pod 1.117M tok/s，256 卡 pod 1.581M |
| Meta 1024 卡故障 | 平均宕机 7.9 小时 |

## 收尾

AI 硬件需求有一个和消费电子不一样的地方：它是自指扩张。模型越大，需要的内存和互连越多；内存和互连越多，能训的模型越大。能打断这个回路的，是电力和模型 1 年的生命周期。

给投研留一个问题：上面那张六层表里，未来 12 个月哪一层的供给曲线最平、需求曲线最陡？
